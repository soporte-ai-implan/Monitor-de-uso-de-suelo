# Monitor de Suelo IMPLAN Torreón

apify-client>=1.7.0        # correr los Actores de scraping
python-dotenv>=1.0.0       # leer .env (token de Apify)
requests>=2.31.0           # geocoding con Nominatim
fastapi>=0.110.0           # API que consume el dashboard
uvicorn[standard]>=0.29.0  # servidor de la API
apscheduler>=3.10.0        # scheduler en proceso (alternativa a cron/Task Scheduler)

# Nota: no se usa shapely ni geopandas a proposito. El point-in-polygon esta
# implementado a mano en zonas.py (~60 lineas) para que el pipeline instale sin
# dependencias binarias/GDAL, que en Windows son la principal fuente de dolor
# al desplegar. Si mas adelante se necesitan operaciones geometricas mas
# pesadas (uniones, buffers), ahi si vale la pena meter shapely.
